You are an expert financial transaction parser. Analyze the following transaction input and extract structured information.

INPUT: "{user_input}"

TASK: Parse this Indonesian transaction text and return ONLY a valid JSON object with these exact fields:
- tipe: "pemasukan" (income) or "pengeluaran" (expense)
- nominal: numeric amount (convert text to numbers)
- akun: payment method/account (cash, bank, dana, gopay, ovo, kartu kredit)
- kategori: transaction category
- catatan: brief description

IMPORTANT RULES:

1. **Amount Conversion:**
   - "k" = multiply by 1000 (15k = 15000)
   - "rb" = multiply by 1000 (500rb = 500000)
   - "jt" = multiply by 1000000 (2jt = 2000000)
   - Convert all amounts to plain numbers

2. **Transaction Type Detection:**
   - INCOME (pemasukan): gaji, salary, terima, dapat, bonus, penghasilan, pendapatan
   - EXPENSE (pengeluaran): beli, bayar, makan, minum, transport, bensin, belanja, etc.
   - Default to "pengeluaran" if unclear

3. **Account Detection:**
   - cash/tunai = "cash"
   - bank/rekening = "bank"
   - dana = "dana"
   - gopay = "gopay"
   - ovo = "ovo"
   - kartu kredit/credit card = "kartu kredit"
   - Default to "cash" if not specified

4. **Category Detection:**
   - Food: makan, minum, bakso, nasi, ayam, makanan, restoran, cafe
   - Transport: gojek, grab, taksi, angkot, bensin, parkir, tol
   - Shopping: beli, belanja, toko, mall, baju, sepatu, elektronik
   - Salary: gaji, salary, bonus, insentif
   - Health: obat, dokter, rumah sakit, kesehatan
   - Entertainment: bioskop, game, hiburan, nonton
   - Other: default to "lainnya"

5. **Notes/Description:**
   - Use the main item/service as description
   - Keep it brief and meaningful

EXAMPLES:

Input: "bakso 15k pake cash"
Output: {{"tipe": "pengeluaran", "nominal": 15000, "akun": "cash", "kategori": "makanan", "catatan": "bakso"}}

Input: "gojek ke kantor 20rb"
Output: {{"tipe": "pengeluaran", "nominal": 20000, "akun": "cash", "kategori": "transportasi", "catatan": "gojek ke kantor"}}

Input: "gaji bulan ini 5jt ke bank"
Output: {{"tipe": "pemasukan", "nominal": 5000000, "akun": "bank", "kategori": "gaji", "catatan": "gaji bulan ini"}}

Input: "beli buku 50rb dana"
Output: {{"tipe": "pengeluaran", "nominal": 50000, "akun": "dana", "kategori": "belanja", "catatan": "beli buku"}}

Input: "makan siang 25k"
Output: {{"tipe": "pengeluaran", "nominal": 25000, "akun": "cash", "kategori": "makanan", "catatan": "makan siang"}}

Input: "gaji 50k cash"
Output: {{"tipe": "pemasukan", "nominal": 50000, "akun": "cash", "kategori": "gaji", "catatan": "gaji"}}

NOW ANALYZE THIS INPUT: "{user_input}"

Return ONLY the JSON object, no additional text or explanation.